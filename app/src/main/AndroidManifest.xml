<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andhikaeffendy.projectorang">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:permissionGroup="android.permission-group.STORAGE"
        android:protectionLevel="dangerous" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="DonasiPanti"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AccountActivity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AccountActivity.SignUpActivity" />
        <activity
            android:name=".MainActivity"
            android:label="Beranda"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".BottomNavigationActivity"
            android:label="DonasiPanti" />
        <activity android:name=".DaftarPantiActivity" />
        <activity android:name=".DetailPantiActivity" />
        <activity android:name=".DonasiActivity"></activity>
    </application>

</manifest>